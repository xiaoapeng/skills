interface:
  display_name: "Git 提交助手"
  short_description: "根据暂存差异生成中文提交信息，并在提交前请求确认"
  default_prompt: "使用 $git-commit-cn-helper，先读取 prompts/git-commit-cn.md 的共享规则，分析已暂存改动，生成中文 commit message，并在执行 git commit 前明确请求确认。"

policy:
  allow_implicit_invocation: true
