---
name: git-commit-cn-helper
description: Analyze staged Git changes and generate a concise Chinese commit message that follows repository commit style. Use when the user asks to commit code, generate a commit message, or summarize staged changes into a Chinese Git commit title and bullets.
---

# Git Commit CN Helper

This file is the installation entry and Codex/OpenAI adapter for the skill.

Read and follow:

- `prompts/git-commit-cn.md`

Use `references/commit_rules.md` when the full rule text or examples are needed.

Never run `git commit` until the user explicitly confirms.
